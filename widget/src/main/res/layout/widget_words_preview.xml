<?xml version="1.0" encoding="utf-8"?>
<!--
  «In words» as the launcher's picker draws it where it asks for a static layout: sample values
  in the default dress, the one-row form of the default 4×1 placement, with the widget's ranks:
  «Steps today» at 16 sp over the count at 39 sp Bold on the leading side; the sentence at
  18 sp Medium and the goal at 16 sp Medium against the far edge.
  Sample data only: a preview is a drawing of the product, never a reading.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_preview_card"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="14dp"
    android:paddingTop="6dp"
    android:paddingEnd="14dp"
    android:paddingBottom="6dp"
    tools:ignore="HardcodedText">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:text="@string/widget_eyebrow"
            android:textColor="@color/widget_preview_ink_dim"
            android:textSize="16sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:singleLine="true"
            android:text="@string/widget_preview_count"
            android:textColor="@color/widget_preview_ink"
            android:textSize="39sp"
            android:textStyle="bold" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="end"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="0dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:gravity="end"
            android:maxLines="2"
            android:text="@string/widget_preview_sentence"
            android:textAlignment="viewEnd"
            android:textColor="@color/widget_preview_ink"
            android:textSize="18sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:singleLine="true"
            android:text="@string/widget_preview_share"
            android:textColor="@color/widget_preview_ink"
            android:textSize="16sp" />
    </LinearLayout>
</LinearLayout>
